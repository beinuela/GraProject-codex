spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/campus_material?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Lyk20041102.
    driver-class-name: com.mysql.cj.jdbc.Driver

security:
  jwt:
    secret: dev-local-jwt-secret-key-must-be-at-least-32-bytes
  cors:
    allowed-origins: http://localhost:5173

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

knife4j:
  enable: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
