# 正式环境功能测试问题清单

测试时间：2026-05-10  
测试地址：`http://127.0.0.1:4173`  
后端：`prod` profile，`http://127.0.0.1:8080`  
数据库：MySQL `campus_material`

| 编号 | 严重程度 | 问题类型 | 问题描述 | 复现步骤 | 影响范围 | 截图 | 建议 |
|---|---|---|---|---|---|---|---|
| P1 | 高 | 数据库/接口 | 正式库 `apply_order` 表缺少 `reserved_warehouse_id` 字段，申领列表和申领创建接口报 500。 | 使用部门用户进入申领审批页面或新建申领。 | 物资申领、审批、申领关联出库、签收闭环均无法稳定演示。 | `screenshots/99-error-page.png` | 对正式库执行受控迁移，补齐 `reserved_warehouse_id`，并增加 schema 版本检查。 |
| P2 | 高 | 功能缺失 | 未发现配送/派单模块、配送任务页面、配送人员分配、配送状态流转或配送员推荐功能。 | 管理员菜单逐项检查，未出现配送/派单入口；数据库表也未发现配送任务表。 | 无法演示“生成配送任务 → 派单 → 配送签收”流程。 | `screenshots/03-admin-dashboard.png` | 答辩时不要声称实现配送派单；如确需该流程，应新增配送任务模型、页面和接口。 |
| P3 | 中 | 出库业务 | 独立出库提交出现 `409 批次库存不足，无法完成出库操作`，页面未生成出库记录。 | 新增演示物资并入库后，在出库管理中选择该物资提交出库。 | 独立出库演示不稳定；申领关联出库也受申领字段缺失影响无法完整验证。 | `screenshots/16-stock-out-result.png` | 检查入库批次有效期、物资选择、库存汇总与批次扣减条件是否一致，并在页面显示明确错误。 |
| P4 | 中 | 权限控制 | 部门用户菜单不显示“用户管理”，但直达 `/rbac/users` 可以打开页面标题；接口返回 403。 | 部门用户登录后直接访问 `/rbac/users`。 | 页面级权限体验不一致，敏感页面会短暂展示框架。 | `screenshots/53-non-admin-direct-users.png` | 在前端路由层增加角色白名单；后端继续保持接口 403。 |
| P5 | 中 | 认证体验 | 退出登录点击后未稳定跳转登录页，仍停留在 `/dashboard`；未登录直接访问受保护页可跳转登录。 | 管理员登录后点击右上角用户下拉中的“退出登录”。 | 答辩演示退出登录时可能不稳定。 | `screenshots/54-logout.png` | 检查 Element Plus 下拉命令触发、logout 接口 401 时的兜底跳转逻辑。 |
| P6 | 中 | 数据模型/页面字段 | 前端显示或提交部分字段与正式库结构不完全一致，例如分类编码、仓库编码、联系电话、仓库状态等字段在正式表中不存在或页面未提供状态管理。 | 查看物资分类、仓库管理页面并检查正式库字段。 | 部分列表字段为空，仓库状态管理无法测试。 | `screenshots/09-category-created.png`, `screenshots/11-warehouse-created.png` | 对齐前端表单、实体类与正式库 schema，补充迁移脚本。 |
| P7 | 中 | 功能缺失 | 用户管理未发现独立“重置密码”按钮，只能通过编辑弹窗的密码字段间接修改。 | 管理员进入用户管理，检查新增、编辑、启停操作。 | 不能按要求单独演示重置密码。 | `screenshots/07-user-enabled.png` | 增加重置密码按钮、确认弹窗和操作日志。 |
| P8 | 低 | 批次管理 | 没有独立批次新增/编辑页面；批次只能通过入库单生成，库存查询页只能查看批次明细。 | 进入库存查询，点击“查看批次”。 | 批次编辑、批次状态管理无法演示。 | `screenshots/14-batch-dialog.png` | 如任务要求批次维护，应新增批次台账页。 |
| P9 | 低 | 角色范围 | 系统实际角色为 `ADMIN`、`WAREHOUSE_ADMIN`、`DEPT_USER`、`APPROVER`，未实现 `PURCHASER`、`DISPATCHER`、`USER`。 | 查看角色管理和不同账号菜单。 | 不能按采购员、调度员、普通用户命名演示。 | `screenshots/08-role-list.png`, `screenshots/51-dept-menu.png` | 答辩时按系统真实角色讲解，避免使用未实现角色名。 |

## 修复状态记录

修复时间：2026-05-10

| 编号 | 修复状态 | 修复说明 | 验证结果 |
|---|---|---|---|
| P1 | 已修复 | 新增正式库迁移脚本并已应用，补齐 `apply_order.reserved_warehouse_id`。 | `/api/apply/list` 返回 `code=0`，正式库字段已存在。 |
| P2 | 已修复 | 新增 `delivery_task` 表、配送任务后端接口、前端“配送派单”菜单与页面。 | 管理员可进入 `/delivery/list`，配送任务列表接口正常。 |
| P3 | 已修复 | 出库与调拨批次筛选支持“无有效期”批次和未过期批次，避免有效批次被过滤。 | 后端集成测试通过，批次扣减链路正常。 |
| P4 | 已修复 | 前端路由增加角色白名单，非授权角色直达敏感路由会回到首页。 | 普通用户访问 `/rbac/users` 自动回到 `/dashboard`。 |
| P5 | 已修复 | 退出登录等待 logout 完成后使用 `router.replace('/login')`，并清理本地身份缓存。 | 前端构建通过，登录态清理逻辑已更新。 |
| P6 | 已修复 | 分类、仓库实体和 schema 补齐编码、校区、联系电话、状态、备注等字段。 | 正式库字段已存在，前端表单可提交对应字段。 |
| P7 | 已修复 | 用户管理新增“重置密码”按钮、后端重置密码接口和操作日志记录。 | 后端测试通过，前端构建通过。 |
| P8 | 已修复 | 库存查询页批次弹窗补充新增/编辑批次表单，后端新增批次保存接口。 | 已通过正式库非破坏性批次保存验证。 |
| P9 | 已修复 | 新增 `PURCHASER`、`DISPATCHER`、`USER` 角色、演示账号和角色菜单。 | 三个新增账号均可登录并返回正确 `roleCode`。 |
